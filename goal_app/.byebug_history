c
allow(view).to receive(:current_user).and_return(hello_world)
allow(view).to receive(current_user).and_return(hello_world)
allow(view).to receive(:current_user).and_return(hello_world)
allow(view).to receive(:current_user)
view
biew
exit
controller
